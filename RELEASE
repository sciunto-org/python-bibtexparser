How to release
==============

* Update CHANGELOG
* Update version in doc/conf.py
* Update the file info.py
* Build doc, push it in the gh-pages branch
* Create a tarball and upload it on github
    git archive master | bzip2 > libzimsciunto-x.y.tar.bz2
* Send the package on pypi
    python setup.py sdist upload
